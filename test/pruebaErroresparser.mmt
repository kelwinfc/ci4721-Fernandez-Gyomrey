[!]int a;                      -- Expresión de índices inválida

[1]!int b;                     -- Tipo inválido

alias c !;                     -- Declaración de alias inválida

const int d = !;               -- Inicializacion de variable 'd' invalida.

int f = !;                     -- Inicializacion de variable 'f' invalida.

int g !;                       -- Declaración de variable debe finalizar con ';'.

int h = !;                     -- Declaración de variable debe finalizar con ';'.


fun none main(){

    if (!){                    -- Expresión de if inválida
    
    }

    while (!){                 -- Expresión de while inválida
    
    }

    for (!){                   -- Expresión de for inválida
    
    }
}

const int e ! 3;               -- Inicializacion de variable 'e' invalida.

fun int i(int a, !){           -- Error en parametro de definicion de funcion.

}